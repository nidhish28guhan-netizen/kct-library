{
  "name": "kct-library",
  "version": "1.0.0",
  "private": true,
  "description": "KCT Library — college central library management system (Node/Express + Electron/Vite/React, binary local-file persistence)",
  "license": "MIT",
  "author": "Nidhish Guhan S",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm run build && npm --prefix backend start",
    "start:api": "npm --prefix backend start",
    "electron:dev": "npm --prefix frontend run electron:dev",
    "test": "npm --prefix backend test",
    "test:e2e": "npm --prefix backend run test:e2e"
  }
}
